import React from "react";
import "./game.scss";

const Game = ({ typed, isValidInput, inputValue, handleChange, words }) => {
  return (
    <>
      <section>GAME</section>
    </>
  );
};

export default Game;
